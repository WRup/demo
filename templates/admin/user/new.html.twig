{% extends 'admin/layout.user.html.twig' %}

{% block body_id 'admin_post_new' %}

{% block main %}
    <h1>{{ 'title.user_new'|trans }}</h1>

    {{ form_start(registrationForm) }}
        {{ form_row(registrationForm.fullName) }}
        {{ form_row(registrationForm.username) }}
        {{ form_row(registrationForm.email) }}
        {{ form_row(registrationForm.plainPassword) }}

        <button type="submit" class="btn btn-primary">
            <i class="fa fa-save" aria-hidden="true"></i> {{ 'label.create_user'|trans }}
        </button>
        <a href="{{ path('lab_admin_users') }}" class="btn btn-link">
            <i class="fa fa-list-alt" aria-hidden="true"></i> {{ 'action.back_to_user_list'|trans }}
        </a>
    {{ form_end(registrationForm) }}
{% endblock %}

{% block sidebar %}
    {{ parent() }}

    {{ show_source_code(_self) }}
{% endblock %}
